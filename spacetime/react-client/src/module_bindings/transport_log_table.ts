// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { TransportLog } from "./transport_log_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `transport_log`.
 *
 * Obtain a handle from the [`transportLog`] property on [`RemoteTables`],
 * like `ctx.db.transportLog`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.transportLog.on_insert(...)`.
 */
export class TransportLogTableHandle {
  tableCache: TableCache<TransportLog>;

  constructor(tableCache: TableCache<TransportLog>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<TransportLog> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `log_id` unique index on the table `transport_log`,
   * which allows point queries on the field of the same name
   * via the [`TransportLogLogIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.transportLog.log_id().find(...)`.
   *
   * Get a handle on the `log_id` unique index on the table `transport_log`.
   */
  log_id = {
    // Find the subscribed row whose `log_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): TransportLog | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.log_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: TransportLog) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: TransportLog) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: TransportLog) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: TransportLog) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: TransportLog, newRow: TransportLog) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: TransportLog, newRow: TransportLog) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
